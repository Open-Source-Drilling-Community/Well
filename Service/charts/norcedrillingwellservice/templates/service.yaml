apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingwellservice.fullname" . }}
  labels:
    {{- include "norcedrillingwellservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingwellservice.selectorLabels" . | nindent 4 }}
