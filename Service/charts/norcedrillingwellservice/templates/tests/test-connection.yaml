apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingwellservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingwellservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingwellservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
